<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AulaComponentesInterface" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Cores primárias -->
        <item name="colorPrimary">@color/main_purple</item>
        <item name="colorPrimaryVariant">@color/main_dark_purple</item>
        <item name="colorOnPrimary">@color/main_white</item>

        <!-- Cores secundarias -->
        <item name="colorSecondary">@color/main_purple</item>
        <item name="colorSecondaryVariant">@color/secondary_gray_blue</item>
        <item name="colorOnSecondary">@color/main_dark_purple</item>

        <!-- Cor barra de status-->
        <item name="android:statusBarColor">@color/main_purple</item>

        <!-- Cor background -->
        <item name="android:windowBackground">@color/main_white</item>
    </style>
    <!-- Estilizacao caixa de texto -->
    <style name="CaixaTexto" parent="TextAppearance.AppCompat.Button">
        <item name="android:textColor">@color/main_dark_purple</item>
    </style>
    
    <style name="Theme.AulaComponentesInterface" parent="Base.Theme.AulaComponentesInterface" />
</resources>